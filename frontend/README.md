## Clinte Getting Started

First, run the development server:

```bash
npm install -f
# or
yarn install -f

```

then start with

```bash
npm run dev
# or
yarn dev

```

Open [http://localhost:4000](http://localhost:4000) with your browser to see the result.
